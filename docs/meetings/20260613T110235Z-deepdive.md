# AgenTeam Deepdive: context-aware-music-recommender
Date: 2026-06-13 11:02

## Health: NO ACTIVE RUN

## External Signals (Researcher)

- Use existing local CSVs first. The repo already has real local data such as `data/allsong_data.csv` and `data/processed_data.csv`; these are enough to run the app after schema normalization.
- Keep Kaggle as an optional acquisition path because it requires user authentication and dataset-specific license checks.
- Treat Wikipedia Billboard scraping as enrichment, not a blocker for the main app. Use it for chart metadata once core recommendations work.
- Context-aware recommendation is aligned with music recommendation research because song suitability changes by listener scenario and can be approximated with tempo, energy, valence, sentiment, and ranking rubrics.
- Lyrics should remain optional. Raw lyrics can create licensing issues, so the pipeline must tolerate missing lyrics and use derived features when lyrics are available.

## Internal Health (Architect)

- High: app needed a single real-data entry path instead of only `examples/sample_tracks.csv`.
- High: real-world CSVs can contain duplicate semantic columns such as `name` and `track_name`; alias normalization must coalesce duplicates safely.
- Medium: Streamlit needed caching around load and feature preparation to avoid recomputing expensive preprocessing on every UI rerun.
- Medium: five tiny placeholder notebooks should be replaced by one end-to-end notebook that imports reusable code from `src/`.
- Low: Kaggle and notebook dependencies are workflow dependencies; they can stay in root requirements for classroom simplicity.

## Recommendations (PM)

1. **Standardize real-data loading path** -- use `scripts/prepare_real_data.py` to normalize existing local CSVs or downloaded Kaggle CSVs into `data/processed/tracks_app_ready.csv`.  
   Priority: high  
   Effort: low

2. **Harden duplicate-column aliasing** -- prefer canonical columns such as `track_name` over aliases such as `name`, then coalesce duplicate columns.  
   Priority: high  
   Effort: medium

3. **Add Streamlit caching** -- cache default dataset loading and feature preparation so real-data demos stay responsive.  
   Priority: high  
   Effort: low

4. **Keep Billboard scraping as enrichment only** -- run the app with music data first, then add scraped chart features for report depth.  
   Priority: medium  
   Effort: medium

5. **Make the merged notebook the single demo path** -- keep one notebook that runs data loading, cleaning, EDA, feature engineering, model evaluation, and recommendation demo.  
   Priority: high  
   Effort: low

## Action Items

- Dev: add `scripts/prepare_real_data.py` and app auto-loading for `data/processed/tracks_app_ready.csv`.
- Dev: add duplicate-column-safe schema normalization in `src/data/load_data.py`.
- Dev: add Streamlit caching for dataset loading and feature preparation.
- PM: replace five placeholder notebooks with `notebooks/context_aware_music_pipeline.ipynb`.
- QA: verify `python -m pytest`, prepare real local data, and smoke-test Streamlit on localhost.
